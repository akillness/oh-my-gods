# Triage

- Problem: choose the next highest-value skill lane after `sprint-retrospective` merged and package it around current hosted-deployment behavior instead of stale one-step deploy claims
- Audience: `oh-my-gods` maintainers and recurring scheduler runs that need one bounded, reviewable skill-improvement lane at a time
- Why now: PR `#78` merged on April 21, 2026, so the lock must leave the closed retro lane and select the next standards-and-trigger gap without reopening merged work
