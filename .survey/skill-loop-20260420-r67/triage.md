# Triage

- Problem: PR `#65` for `git-submodule` is already merged, so the recurring loop
  needs a fresh survey-backed next lane instead of a stale PR-review pass.
- Audience: `oh-my-gods` maintainers and downstream agent hosts that benefit
  from portable, triggerable, eval-backed skill directories.
- Why now: GitHub's current `gh skill` distribution surface increases the value
  of packaging quality, and `api-documentation` remains a broad single-file
  skill with no focused `references/` or `evals/`.
