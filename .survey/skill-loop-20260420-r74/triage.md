# Triage

- Problem: `log-analysis` is still a generic recipe surface with weak route-outs and no support package, even though it sits on the hot path between monitoring, debugging, and deployment incidents.
- Audience: `oh-my-gods` maintainers and agent hosts that need clean runtime-log triage before deeper debugging or observability work.
- Why now: PR `#69` merged, so the loop needs the next bounded lane, and current ecosystem signal still favors portable, progressive-disclosure skill packaging over oversized inline recipes.
