# Triage

- Problem: The recurring skill loop needs the next bounded packaging lane after `environment-setup` merged, and the strongest current repo-local gap is `user-guide-writing` as a large overlap-prone single-file skill with no `references/` or `evals/`.
- Audience: Maintainers of `oh-my-gods` and downstream coding-agent users who need portable, route-clean customer-documentation skills.
- Why now: PR `#71` is already merged, so the loop must re-enter survey mode and advance to the next reviewable lane instead of reopening the closed `environment-setup` work.
