# Settings

- Keep the skill compact and progressive-disclosure friendly
- Use local `references/` and `evals/` before any deeper optimization loop
- Do not add assets or scripts unless the target shows a concrete reuse need

# Rules

- Keep merged lanes closed unless new review evidence reopens them
- Package one bounded lane per run
- Prefer route-clean wording over broad example dumps
- Keep `pattern-detection` focused on repeated shapes and anomalies rather than
  raw log triage, debugging, or stakeholder KPI analysis

# Hooks

- Run repo-wide validation after edits
- Run per-skill validation for the active lane
- Review the branch for duplicate work before opening the PR path
