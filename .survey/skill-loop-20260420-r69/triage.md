# Triage

- Problem: choose the next highest-value skill lane after PR `#66` merged and package the target with clearer boundaries plus reusable support files
- Audience: maintainers of `oh-my-gods` and agent hosts that rely on portable, discoverable workflow skills
- Why now: the lock still pointed at `api-documentation` review, but the merge closed that lane and opened room for the next bounded improvement pass
