# Triage

- Problem: choose the next bounded recurring skill-packaging lane after
  `task-estimation` merged, then package that lane with sharper routing and
  reusable support files if justified
- Audience: `oh-my-gods` maintainers and downstream agents that rely on
  PM-adjacent execution skills to run short coordination rituals without
  colliding with planning, estimation, or retrospective workflows
- Why now: PR `#76` is merged on `main`, so this run should return to survey
  mode, pick the next highest-value generic PM lane, and advance it to PR-open
  if the bounded change set validates cleanly
