# Triage

- Problem: after PR `#63` merged the `code-refactoring` lane, `api-design` became the strongest remaining general-purpose legacy skill in `oh-my-gods`: generic trigger wording, no focused `references/`, and no `evals/` to prove contract-design boundaries.
- Audience: `oh-my-gods` maintainers and downstream agents that need reliable routing for REST, GraphQL, OpenAPI, and API-contract requests before implementation starts.
- Why now: as of April 19, 2026, PR `#63` is merged, there are no open PRs in the repo, validation is clean, and the recurring loop should advance immediately to the next bounded packaging lane instead of re-surveying already-closed skills.
