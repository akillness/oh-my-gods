# Triage

- Problem: after PR `#64` merged the `api-design` lane, the recurring loop needs a fresh survey-backed decision on the next highest-value improvement target and a bounded support-package pass instead of reworking closed lanes.
- Audience: `oh-my-gods` maintainers and downstream agent hosts that benefit from portable, triggerable, eval-backed skill directories.
- Why now: the previous merge path is complete, the lock state drifted behind GitHub, and `git-submodule` remains a large workflow-critical skill with no focused `references/` or `evals/`.
