# Triage

- Problem: after PR `#62` closed the `security-best-practices` lane, `code-refactoring` became the strongest remaining high-frequency legacy skill in `oh-my-gods`: broad trigger wording, inline-heavy instructions, no `references/`, and no `evals/`.
- Audience: `oh-my-gods` maintainers and downstream agents that need reliable routing for cleanup, refactor, and deslop requests.
- Why now: as of April 19, 2026, PR `#62` is merged, repo validation is clean, and the recurring loop should move immediately onto the next bounded packaging lane instead of re-surveying already-closed skills.

