# Triage

- Problem: The recurring skill loop needs the next bounded lane after `user-guide-writing` merged, and the strongest remaining packaging gap is `data-analysis` as a still-generic, support-file-light skill with weak route-outs.
- Audience: Maintainers of `oh-my-gods` and downstream coding-agent users who need structured-dataset analysis to route cleanly across data, observability, and BI tasks.
- Why now: PR `#72` is already merged on `main`, so the loop must refresh the survey lock and advance to the next reviewable lane instead of reopening the closed user-doc work.
