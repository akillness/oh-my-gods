# Triage

- Problem: The recurring skill loop needs the next bounded packaging lane after `log-analysis` merged, and the strongest remaining repo-local gap is `environment-setup` as a large single-file skill with no eval-backed trigger surface.
- Audience: Maintainers of `oh-my-gods` and downstream coding-agent users who depend on portable, standards-clean skills.
- Why now: PR `#70` is already merged, so this run must stop treating `log-analysis` as active work and move the loop to the next measurable lane.
