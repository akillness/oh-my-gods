# Triage

- Problem: choose the next bounded recurring skill-packaging lane after
  `pattern-detection` merged, then package that skill with clearer routing and
  reusable support files if justified
- Audience: `oh-my-gods` maintainers and downstream agents that rely on
  planning-oriented skills to turn broad requests into executable work
- Why now: the previous lane is closed on `main`, so this run should return to
  survey mode, pick one remaining high-leverage generic skill, and advance it
  to PR-open if the bounded change set validates cleanly
