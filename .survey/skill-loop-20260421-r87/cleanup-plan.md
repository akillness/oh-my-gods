# Cleanup Plan

## Target

- `sprint-retrospective`

## Why this lane still needs a review pass

- PR `#78` is already open, so the recurring loop contract requires a branch
  review before merge.
- The skill already has support files, but the main `SKILL.md` still misses the
  recommended `Best practices` and `References` sections.

## Planned bounded changes

1. Add `Best practices` and `References` headings to
   `.god-skills/sprint-retrospective/SKILL.md`.
2. Record this review run in `.survey/skill-loop-20260421-r87/`.
3. Update the shared lock files so the lane moves from `pr-open` to `merge`.

## Explicit non-goals

- No new assets
- No new scripts
- No `skill-autoresearch` loop
- No unrelated repo-wide skill rewrites
